import { NgModule } from '@angular/core';
import { IonicPageModule } from 'ionic-angular';
import { InvolvedPassengerPage } from './involved-passenger';

@NgModule({
  declarations: [
    InvolvedPassengerPage,
  ],
  imports: [
    IonicPageModule.forChild(InvolvedPassengerPage),
  ],
})
export class InvolvedPassengerPageModule {}
