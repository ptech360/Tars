import { NgModule } from '@angular/core';
import { IonicPageModule } from 'ionic-angular';
import { InvolvedDriverPage } from './involved-driver';

@NgModule({
  declarations: [
    InvolvedDriverPage,
  ],
  imports: [
    IonicPageModule.forChild(InvolvedDriverPage),
  ],
})
export class InvolvedDriverPageModule {}
